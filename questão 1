#include <stdio.h>

void somarSemSinal(int n, int A[], int B[], int C[]) {
    int carry = 0;
    for (int i = n - 1; i >= 0; i--) {
        int soma = A[i] + B[i] + carry;
        C[i + 1] = soma % 2;
        carry = soma / 2;
    }
    C[0] = carry; // Armazena o transporte final
}
